# Auth Screen — User Experience & Interaction Flow

```
[User Arrives at /login]
         │
         ├──► Selects "Passkey / Face ID" ──► [Biometric Prompt] ──► [Success: Redirect to /owner/dashboard]
         │
         ├──► Types Credentials ──► [Validation Check] ──► [Submit OPButton] ──► [Redirect to /owner/dashboard]
         │
         └──► Clicks "Kayıt Ol" ──► [Smooth Tab Switch to Progressive Multi-Step Onboarding Form]
```
