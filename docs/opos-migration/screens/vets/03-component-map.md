# Vets Domain — Component Map

```
VetsPageLayout
├── PageHeader (Title: "Veteriner Hekim & Klinik Bul")
├── SearchBar -> FormField (16px Input Lock)
├── ClinicList
│   └── ClinicCard -> HealthCard (cat-vet)
└── AppointmentModal -> FormModal
```
