/**
 * ODI.PET — PLANNING CORE COMPATIBILITY RE-EXPORT
 * ===============================================
 * Single Canonical Type Definition re-exported to prevent type fragmentation.
 * All canonical planning types are rooted in `@/lib/plans/types`.
 */

export * from '@/lib/plans/types';
